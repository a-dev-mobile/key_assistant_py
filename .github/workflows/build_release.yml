name: Build and Release Executables

on:
  push:
    tags:
      - 'v*.*.*'
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller PyYAML==6.0.1 evdev==1.7.1 keyboard==0.13.5

    - name: Build executable
      run: |
        pyinstaller --onefile --hidden-import=pynput.keyboard._xorg src/main.py

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ubuntu-executable
        path: dist/

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Get version
      id: get_version
      run: echo "::set-output name=version::${GITHUB_REF##*/}"

    - name: Create GitHub Release
      if: startsWith(github.ref, 'refs/tags/')
      id: create_release
      uses: actions/create-release@v1.1.4
      env:
        GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        draft: false
        prerelease: false

    - name: Download Ubuntu Artifact
      uses: actions/download-artifact@v4
      with:
        name: ubuntu-executable
        path: dist/ubuntu-latest

    - name: Upload Ubuntu Release Asset
      if: startsWith(github.ref, 'refs/tags/')
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: dist/ubuntu-latest/main
        asset_name: keyseq-${{ steps.get_version.outputs.version }}.linux
        asset_content_type: application/octet-stream
